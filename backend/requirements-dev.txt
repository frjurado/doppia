# Inherits all production dependencies
-r requirements.txt

# Graph visualization (dev/debug)
pyvis==0.3.2
networkx==3.4.2

# Linting & formatting
black==24.10.0
isort==5.13.2
ruff==0.8.4

# Pre-commit hooks
pre-commit==4.0.1

# Testing
# pytest 9 + pytest-asyncio 1.x (Component 10 Step 15): clears PYSEC-2026-1845
# (dev-only); pytest-asyncio 0.24's `pytest<9` pin was the blocker.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Dependency auditing (also run report-only in CI)
pip-audit==2.10.1
