# Mockups

**`opus_urtext/DESIGN.md` is the authoritative design system for all frontend work.**
Read it before writing any UI code (see `CLAUDE.md` § Frontend design system).

The three view mockups below are **historical** (pre-build explorations, 2026-04/05).
The shipped UI has since diverged — nav bar redesign, corpus/fragment browser and
fragment viewer remediation, and the cross-surface coherence review all landed in
Component 9 (Steps 12–17). Do not treat these as specs; they are kept as design
provenance only.

| Mockup | Status |
|---|---|
| `score_library_browser/` | Historical — superseded by the shipped corpus browser (Component 2 + Component 9 Step 13) |
| `score_viewer_player/` | Historical — superseded by the shipped score viewer (Component 3 + Component 9 Step 16) |
| `harmony_quiz_view/` | Historical — exercise UI is Phase 2 scope; no implementation exists |
